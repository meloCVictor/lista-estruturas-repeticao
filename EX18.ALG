algoritmo "Ex18"
var
  senha: caractere
inicio
  repita
    escreva("Crie uma senha (mínimo 6 caracteres): ")
    leia(senha)
  ate comprimento(senha) >= 6
  escreval("Senha cadastrada com sucesso.")
fimalgoritmo
